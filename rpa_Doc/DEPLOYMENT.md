# 🚀 คู่มือการนำขึ้น Windows Server (Production Deployment)

เอกสารนี้รวบรวมขั้นตอนการติดตั้งโปรเจกต์ **RPA & Smart Tax API** บน Windows Server โดยใช้ **NSSM** ซึ่งเป็นวิธีที่ง่ายและเสถียรที่สุดในการรัน Python เป็น Windows Service

---

## 🏗️ ทางเลือกการติดตั้ง (Deployment Choice)

เราเลือกแนะนำการใช้ **NSSM (Non-Sucking Service Manager)** เนื่องจากติดตั้งง่ายกว่า IIS และรองรับการจัดการ Process ของ Python ได้อย่างดีเยี่ยม

### ✅ **แนะนำ: ใช้ NSSM (Windows Service)**

- **ข้อดี**: ติดตั้งง่ายมาก (เพียงไม่กี่คำสั่ง), เบาเครื่อง, Auto-restart อัตโนมัติเมื่อโปรแกรมค้างหรือ Server รีบูต, ไม่ต้องตั้งค่า Web Server ซับซ้อน

---

## 🛠️ ขั้นตอนการติดตั้งด้วย NSSM (Step-by-Step)

### 1. เตรียม Server (Prerequisites)

1. ติดตั้ง **Python 3.10+** (ติ๊ก ✅ Add to PATH ตอนลงด้วย)
2. ดาวน์โหลด **[NSSM](https://nssm.cc/download)** และแตกไฟล์ไว้ในเครื่อง (เช่น `C:\nssm`)
3. สร้างโฟลเดอร์สำหรับโปรเจกต์ (เช่น `C:\RPA_API`) และนำไฟล์โปรเจกต์ทั้งหมดไปวาง

### 2. ติดตั้ง Library และสภาพแวดล้อม

เปิด PowerShell (Run as Administrator) แล้วรัน:

```powershell
cd C:\RPA_API
pip install -r requirements.txt
playwright install chromium
```

### 3. สร้าง Windows Service ด้วย NSSM

1. เปิด CMD หรือ PowerShell (Run as Administrator) ไปที่โฟลเดอร์ที่เก็บ `nssm.exe`
2. รันคำสั่งเพื่อเปิดหน้าตั้งค่า:

   ```cmd
   nssm install RPA_Smart_API
   ```

3. ในหน้าต่างที่เด้งขึ้นมา ให้ตั้งค่าดังนี้:
   - **Path**: เลือก `python.exe` (เช่น `C:\Users\Admin\AppData\Local\Programs\Python\Python310\python.exe`)
   - **Startup directory**: เลือกโฟลเดอร์โปรเจกต์ (`C:\RPA_API`)
   - **Arguments**: ใส่คำสั่งรัน server:

     ```text
     main.py
     ```

4. กด **Install service**

### 4. เริ่มการทำงานของ Service

รันคำสั่ง:

```cmd
nssm start RPA_Smart_API
```

ตอนนี้ระบบจะรันเป็นเบื้องหลัง (Background) ตลอดเวลา

---

## 🧪 การทดสอบ (Verification)

เปิด Browser และเข้า `http://localhost:8000/docs`

- ✅ ต้องเจอหน้า **Swagger UI**
- ✅ ลองเรียก API `/rag/ask` เพื่อยืนยันว่า AI ทำงานปกติ

---

## ❓ การดูแลรักษา (Maintenance)

- **ดูสถานะ**: `nssm status RPA_Smart_API`
- **หยุดการทำงาน**: `nssm stop RPA_Smart_API`
- **เริ่มใหม่ (Restart)**: `nssm restart RPA_Smart_API`
- **แก้ไขการตั้งค่า**: `nssm edit RPA_Smart_API`
- **ดู Log**: สามารถเช็คได้ในหน้าต่าง NSSM Tab "I/O" เพื่อ Redirect `stdout` และ `stderr` ไปยังไฟล์ไฟล์ในโฟลเดอร์ `logs` ได้

---

> **Note**: หากในอนาคตต้องการใช้ IIS เป็น Reverse Proxy เพื่อทำ Domain Name หรือ HTTPS สามารถตั้งค่า IIS ให้ทำ Proxy Pass มายัง Port `8000` ได้เช่นกัน
