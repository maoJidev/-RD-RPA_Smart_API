# กองกฎหมาย - RPA RD Scraper & ChatBot

โปรเจกต์นี้เป็นเครื่องมือสำหรับรวบรวมข้อมูลเอกสารข้อหารือภาษีอากรจากเว็บไซต์กรมสรรพากร (RD) และระบบ ChatBot สำหรับสอบถามข้อมูลจากเอกสารที่รวบรวมได้ โดยใช้เทคโนโลยี RPA (Robocorp/Playwright) และ AI (Ollama/LLAMA 3.2)

---

## 🛠 HOW TO SET UP (การติดตั้ง)

ก่อนจะรันโปรเจกต์นี้ คุณจำเป็นต้องติดตั้งเครื่องมือหลักๆ ดังนี้:

### 1. ติดตั้ง RCC (Robocorp Command Chain)

RCC เป็นตัวจัดการ Environment และรัน Robot Tasks

- **Windows**: ดาวน์โหลด rcc.exe และเก็บไว้ที่เครื่อง หรือติดตั้งผ่าน VS Code Extension
- **Robocorp Code Extension**: แนะนำให้ลงใน **VS Code** เพื่อความสะดวกในการรันและดีบั๊ก

### 2. ติดตั้ง Ollama (สำหรับ AI ChatBot)

ต้องมี Ollama ติดตั้งอยู่ในเครื่องเพื่อรันโมเดล LLM แบบ Local

1. ดาวน์โหลดและติดตั้งจาก [ollama.com](https://ollama.com/)
2. เปิด Terminal และพิมพ์คำสั่งเพื่อดึงโมเดล:

   ```bash
   ollama pull llama3.2:1b
   ```

### 3. ไลบรารีที่จำเป็น (Dependencies)

โปรเจกต์นี้จัดการไลบรารีผ่าน `conda.yaml` ซึ่งจะถูกติดตั้งอัตโนมัติเมื่อรันผ่าน RCC ไลบรารีหลักที่ใช้คือ:

- `rpaframework`: สำหรับ Browser Automation
- `playwright`: เครื่องมือควบคุม Browser
- `scikit-learn`: สำหรับการทำ TF-IDF search ใน ChatBot
- `requests`: สำหรับดึงข้อมูล API

---

## 🚀 HOW TO RUN (วิธีการรัน)

โปรเจกต์แบ่งการทำงานออกเป็น 5 Stage หลักในส่วนของ Scraper และ 1 ส่วนของ ChatBot

### 1. การรัน Scraper (เพื่อดึงข้อมูล)

คุณสามารถรันแยกเป็น Task ได้ตามลำดับ (1 -> 5)

| Stage | คำสั่งรัน (RCC) | คำอธิบาย |
| :--- | :--- | :--- |
| **Stage 1** | `rcc run -t run_year` | เก็บรายการปี พ.ศ. ของข้อหารือ |
| **Stage 2** | `rcc run -t run_month` | เก็บรายการเดือนในแต่ละปี |
| **Stage 3** | `rcc run -t run_collect_month_urls_task` | เก็บลิงก์ของเอกสารทั้งหมด |
| **Stage 4** | `rcc run -t run_read_document_content_task` | เข้าไปอ่านเนื้อหาในแต่ละลิงก์ |
| **Stage 5** | `rcc run -t run_filter_documents_task` | กรองและรวมข้อมูลลงในไฟล์ JSON |

### 2. การรัน ChatBot

หลังจากที่รัน Scraper ครบ 5 ขั้นตอนแล้ว จะได้ไฟล์ข้อมูลใน `output/` ให้รัน ChatBot ด้วยคำสั่ง:

```bash
python chatbot_cli_chunked_fixed.py
```

*หมายเหตุ: ต้องรัน Ollama ทิ้งไว้ในเครื่องก่อน*

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

สำหรับการส่งต่องาน นี่คือไฟล์สำคัญที่คุณต้องรู้:

- `rpa_Doc/` : โฟลเดอร์หลักของโปรเจกต์
  - `tasks.py`: ไฟล์รวมชุดการรัน (Task Entries)
  - `year_collector.py`, `month_collector.py`...: ไฟล์ Logic ของแต่ละ Stage
  - `chatbot_cli_chunked_fixed.py`: ตัวรันระบบแชท AI
  - `conda.yaml`: ตั้งค่าไลบรารีและ Python Version (3.11)
  - `robot.yaml`: ตั้งค่าโครงสร้างโปรเจกต์สำหรับ Robocorp
  - `output/`: (จะถูกสร้างเมื่อรัน) เก็บไฟล์ JSON ข้อมูลที่ดึงมาได้

---

## ⚠️ ขั้นตอนที่ต้องทำก่อนจะรัน

1. **เช็คอินเทอร์เน็ต**: เพราะต้องเข้าถึงเว็บไซต์กรมสรรพากร
2. **เช็คโฟลเดอร์ Output**: ตรวจสอบว่าระบบมีสิทธิ์เขียนไฟล์ในเครื่อง
3. **เช็ค Ollama**: พิมพ์ `ollama list` ในเครื่องว่ามี `llama3.2:1b` อยู่หรือไม่
4. **ลำดับการรัน**: ห้ามข้าม Stage หากรันครั้งแรก เพราะข้อมูล Stage หลังจะต้องการผลลัพธ์จาก Stage ก่อนหน้าเสมอดังนี้:
   `years.json` -> `months.json` -> `month_urls.json` -> `all_document_contents.json` -> `month_document_contents_filtered.json`

---

## ✅ ผลลัพธ์

เมื่อรันสำเร็จ คุณจะได้ผลลัพธ์ใน `rpa_Doc/output/log.html` เพื่อดูสถานะการทำงาน และไฟล์ JSON สำหรับนำไปใช้งานต่อในระบบอื่นๆ
